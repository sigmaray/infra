PROXY_USER=proxy
PROXY_PASSWORD=change-me-proxy-password

# Host ports (bound to 127.0.0.1 only — not exposed to the public internet).
HTTP_PROXY_PORT=3128
SOCKS_PROXY_PORT=1080

# 3proxy.cfg is generated from this file: ./scripts/generate-3proxy-cfg.sh

# Proxy URL examples (see README.md):
# HTTP:  http://proxy:<PROXY_PASSWORD>@127.0.0.1:3128
# SOCKS: socks5://proxy:<PROXY_PASSWORD>@127.0.0.1:1080
