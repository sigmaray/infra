POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-postgres-password

# Host port (bound to 127.0.0.1 only — safe for Docker apps via host.docker.internal).
POSTGRES_PORT=5432

# DATABASE_URL example for connecting to the database from another container:
# postgresql://postgres:<POSTGRES_PASSWORD>@host.docker.internal:5432/weather
