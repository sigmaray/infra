# Host bind for direct access (Caddy uses static-web:80 on the infra network).
# 127.0.0.1 — local access only (recommended when using reverse-proxy)
# 0.0.0.0   — accept connections from the internet
STATIC_WEB_BIND_ADDRESS=127.0.0.1
STATIC_WEB_PORT=8080
